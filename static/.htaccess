<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /mise/

    # Don't rewrite files or directories
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Rewrite everything else to 404.html
    RewriteRule ^ 404.html [L]
</IfModule>
